<p id="notice"><%= notice %></p>

<h1>Episodes</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @episodes.each do |episodes| %>
      <tr>
        <td> <td><%= link_to episodes.name, episode_path(episodes.season_id) %></td> </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Back', series_path %>